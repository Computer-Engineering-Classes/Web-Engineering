@model IEnumerable<Aula3.Models.FileViewModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-controller="Home" asp-action="Upload">Submit New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Size)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Size)
            </td>
            <td>
                <a asp-controller="Home" asp-action="Download" asp-route-id=@item.Name target="_blank">Download</a>
            </td>
            <td>
                <a asp-controller="Home" asp-action="Delete" asp-route-id=@item.Name onclick="return confirm('Are you sure?')">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
<p>Number of Documents: @Model.Count()</p>
<p>Total Bytes: @Model.Sum(e => e.Size)</p>